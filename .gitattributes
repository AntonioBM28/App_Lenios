# Normaliza finales de línea: el repo siempre guarda LF, sin importar el
# sistema operativo o editor que haga el checkout (evita diffs falsos por
# CRLF/LF cuando se trabaja entre Windows y Linux/CI).
* text=auto eol=lf
